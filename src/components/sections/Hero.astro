---
import { Image } from 'astro:assets';
import Icon from '../ui/Icon.astro';
import { useTranslations, localizePath, type Lang } from '../../i18n/ui';
import { club } from '../../content/club';
import { img } from '../../lib/images';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section class="relative flex min-h-[92vh] items-center overflow-hidden bg-navy">
  <!-- Fondo aéreo del club con parallax -->
  <div id="hero-bg" class="absolute inset-0 will-change-transform">
    <Image
      src={img('aerial-hero.jpg')}
      alt="Magaluf Sport Club - vista aérea"
      widths={[768, 1280, 1920, 2400]}
      sizes="100vw"
      class="h-[115%] w-full object-cover"
      loading="eager"
      fetchpriority="high"
    />
  </div>
  <div class="absolute inset-0 bg-gradient-to-br from-navy/90 via-navy/70 to-navy/45"></div>
  <div class="absolute inset-0 bg-gradient-to-t from-navy via-transparent to-transparent"></div>

  <div class="relative mx-auto w-full max-w-6xl px-5 pt-28 pb-16 sm:px-8">
    <div class="max-w-2xl text-white" data-reveal>
      <!-- Logo + nombre del club -->
      <div class="flex items-center gap-3">
        <Image src={img('logo.png')} alt="Magaluf Sport Club" width={96} height={84} class="h-14 w-auto" loading="eager" />
        <span class="font-display text-lg font-extrabold leading-tight">Magaluf<br />Sport Club</span>
      </div>

      <h1 class="mt-7 text-4xl font-extrabold leading-[1.02] sm:text-5xl md:text-6xl">{t('hero.title')}</h1>
      <p class="mt-3 font-display text-base font-semibold text-gold sm:text-lg">{t('hero.brandline')}</p>
      <p class="mt-5 max-w-xl text-lg leading-relaxed text-white/85">{t('hero.subtitle')}</p>

      <!-- Accesos rápidos: Pádel · Fútbol 7 · Reservar · WhatsApp -->
      <div class="mt-8 flex flex-wrap gap-3">
        <a href={localizePath(lang, '/padel')} class="inline-flex items-center gap-2 rounded-full bg-white/10 px-6 py-3.5 font-display font-semibold text-white ring-1 ring-white/25 backdrop-blur transition-all hover:bg-white/20">
          <Icon name="padel" size={20} />{t('nav.padel')}
        </a>
        <a href={localizePath(lang, '/futbol-7')} class="inline-flex items-center gap-2 rounded-full bg-white/10 px-6 py-3.5 font-display font-semibold text-white ring-1 ring-white/25 backdrop-blur transition-all hover:bg-white/20">
          <Icon name="football" size={20} />{t('nav.football')}
        </a>
        <a href={club.booking} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 rounded-full bg-gold px-6 py-3.5 font-display font-bold text-navy shadow-lg shadow-gold/25 transition-all hover:-translate-y-0.5 hover:bg-accent-dark">
          {t('hero.ctaPrimary')}
          <Icon name="arrow" size={18} />
        </a>
      </div>

      <!-- Contacto directo: dirección · teléfono · WhatsApp -->
      <div class="mt-8 flex flex-wrap items-center gap-x-6 gap-y-3 text-sm text-white/85">
        <a href={club.mapsLink} target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 hover:text-gold">
          <Icon name="pin" size={16} />{club.address}
        </a>
        <a href={`tel:${club.phone}`} class="flex items-center gap-2 hover:text-gold">
          <Icon name="phone" size={16} />{club.phoneDisplay}
        </a>
        <a href={`mailto:${club.email}`} class="flex items-center gap-2 hover:text-gold">
          <Icon name="mail" size={16} />{club.email}
        </a>
        <a href={`https://wa.me/${club.whatsapp}`} target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 font-semibold text-[#25D366] hover:brightness-110">
          <Icon name="whatsapp" size={16} />WhatsApp
        </a>
      </div>
    </div>
  </div>
</section>

<script>
  const bg = document.getElementById('hero-bg');
  if (bg && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    let ticking = false;
    const update = () => {
      const y = window.scrollY;
      if (y < window.innerHeight) bg.style.transform = `translate3d(0, ${y * 0.25}px, 0)`;
      ticking = false;
    };
    window.addEventListener('scroll', () => {
      if (!ticking) { requestAnimationFrame(update); ticking = true; }
    }, { passive: true });
  }
</script>
